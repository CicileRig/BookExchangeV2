<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bcs.bookexchangev2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name="activities.MainActivity" >
        </activity>
        <activity android:name="activities.RegistrationActivity" />
        <activity android:name="activities.ProfilActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="activities.NavigationActivity">
        </activity>
        <activity android:name="activities.RegistrationActivity2" />
        <activity android:name="activities.APIActivity" />
        <activity android:name="activities.Add_Book_Activity" />
        <activity android:name="activities.Add_Event_Activity"></activity>
    </application>

</manifest>